# Task 6 — portable runtime/scientific consumer recovery

Status: implementation candidate fixed, ready for independent whole-task review; not integrated, not pushed, not simulator-accepted.

## Exact revision and ownership

- Worker: `../SEA-Nav-Code-batch6`, detached HEAD `844514929726a1cade3303867cc11702b710a04d`.
- Review BASE: `65dcbbc2b13c92af99a9e7d4cba4110880f9b509`.
- Full serial range: `65dcbbc2b13c92af99a9e7d4cba4110880f9b509..844514929726a1cade3303867cc11702b710a04d`.
- Commits, in order:
  1. `6e097ac3d87554a8baf537aa733d72b3c7e70b33` — shared reward and timestamped perception contracts.
  2. `7945a9e6037fee5045cbc12ac59a054ad958b43b` — per-environment integer policy-tick scheduling after independent float32 review.
  3. `844514929726a1cade3303867cc11702b710a04d` — actual runtime input/application, trace/evidence, and filter recovery.
- Author/committer: repository-scoped `TNHTH <174231229+TNHTH@users.noreply.github.com>`. No refs outside this detached worker were changed.
- Sole source writer: `/root/implement_batch6`. Child edge reviewer was read-only and returned findings in messages; root owns independent whole review.
- Registered original inventory plus two controller-approved minimal extensions: historical adapter YAML assertions in `tests/test_experiment_config.py`, and alpha recurrence/output separation in `adapters/command_delay.py`.
- Source/tests are committed and unchanged after final verification. Only worker `task_plan.md` and `resume_state.json` registration remain unstaged; they must not be cherry-picked or committed as shared state. This report is the single authorized primary-checkout write.
- Precedence: the committed log's later sections titled “Current worker truth (2026-09-07 continuation)” and “Recovery checkpoint after interruption”, including “HEAD remains 7945a9e”, describe **earlier historical checkpoints**. The three-commit list and post-commit `8445149` evidence in this report supersede those headings. The fixed source candidate is not changed merely to rename historical log sections during independent review.

## Current truth / evidence boundary

| Field | Current evidence | Limit |
|---|---|---|
| Source | fixed `844514929726a1cade3303867cc11702b710a04d` | not yet accepted by independent whole review |
| Post-commit full CPU suite | 294 passed in 27.21 s | eager CPU + static contracts only |
| Portable Gate A | passed_with_blockers; five CPU/static passes, four separate dependency/runtime blockers | not a clean final project-candidate Rung 2 run |
| Python compatibility | Python 3.8 grammar, all 83 tracked Python files | does not prove every optional dependency supports Python 3.8 |
| Actual simulator/controller | not launched; explicit prerequisite blocker | no fake modules, fake simulator or controller acceptance |
| Paper / training / deployment | no accepted paper identity, no formal metrics, no robot execution | only Tasks 1–6 local recovery source |

## What changed and why

### Shared scientific contracts and actual consumers

`rsl_rl/navigation_reward.py` implements the seven raw terms with explicit upstream versus paper-diagnostic formulas. The independent scalar oracle table drives tests; expectations are not generated by the implementation. Gym consumes raw terms using its inherited once-prepared dt-scaled coefficients; all three adapter entries call the shared terms and apply dt once. Reward owns current physical goal geometry, not held sensor goal data. Gym applies raw weights, contact-compatible terms, stay threshold, commands and horizon before its task constructor/reward preparation. Unsupported active extra reward terms and nonintegral policy-tick horizons are rejected.

`rsl_rl/perception_delay.py` provides preallocated, masked timestamped acquisition/hold state. Upstream acquires each policy tick and refreshes every 100 ms using the source two/three-tick-old choices; separate paper-diagnostic math models 10 Hz acquisition with uniform transport latency. Reset is synthetic, noise-free step 0; neighboring rows are unchanged. Integer epoch-relative scheduling closes the independently reproduced float32 missed-tick defect. Sample timestamps remain actual input timestamps, not invented ideal wall time. Actual Gym and all three adapter observation/reset callers use the helper. Trace now distinguishes sampled latency from actual held sample age at the observation/decision timestamp.

`rsl_rl/environment_profile.py` deep-materializes nested class configuration per run without discarding ordinary runtime-contract methods; keeps immutable resolved dataclasses intact; applies the Task 4 real constructor converters and Task 5 replay/ACSI bridge. Replay reconstruction delta is required even when replay is disabled. Adapters reconcile actual constructor options (including learning rate, entropy, std and geometry) and environment settings with preflight. Ordinary PPO now consumes ten-tick position history, shared ACSI collision-onset probability and normal-reset curriculum; it remains explicitly one-environment/no-replay. Smoke stops at the first selected done and is not an evaluator.

All three adapter mains no longer purge the bundled `rsl_rl` after preflight. A foreign preloaded package is rejected; actual preloaded core/config type identity is retained. Raw checkpoint loads and post-construction `cbf_layer` replacement were removed from guarded startup; there is one profile-aware construction path.

### Portable startup and evidence

`rsl_rl/runtime_preflight.py` owns simulator-free typed YAML/CLI/path requests, independent of adapters. Actual adapter entry modules expose pure `build_parser()` / `preflight()` before guarded `AppLauncher`. Gym's parent remains Torch-free through preflight; an isolated child exercises the real Torch-dependent converters, preserving Preview 4's Gym-before-Torch requirement. Native Gym flags are validated in pure argparse, unknown/unsupported flags rejected, supported device/default choices reconciled with the real native parser, and per-run num_envs/seed/replay options are applied before environment construction. The real registered Gym algorithm conversion is also checked before its first environment constructor.

The YAML is executable input with explicit schema/profile/delta/runtime fields, replacing inert historical metadata. Historical `adapter_manifest.json` identity/hash/unverified tests were preserved. Unsupported paper identity, raw checkpoint flags, nonfresh loading modes, invalid finite/type/range/mini-batch settings, conflicting actor FOV/footprint/stay/filter/delay values, and unsafe outputs fail before proprietary startup/output creation.

Explicit assets are `asset_root/go2.usd` for Lab and `asset_root/ctrl_model/{encoder_vel.jit,encoder_latent.jit,body_latest.jit}`. Presence/size/hash are local integrity inventory only. Unknown controller interface/provenance sets `runtime_ready=False`; real startup is intentionally blocked before application/controller loads. No machine-specific fallback assets or credentials are introduced.

The shared evidence publisher binds resolved config/hash, runtime stack, canonical runtime paths, effective arguments/environment and their hashes, actual constructor receipt, status and closed-file physical JSONL counts. The adapter lazily forwards these helpers, retaining standalone manifest imports without `rsl_rl`. Gym uses shared publication directly, never a Gym-to-adapter import. A dependency import or projection can never become runtime passed. Trace enforces four action stages and records collection-owned actor state, cloning before physical step/reset. Failed physical verification preserves the claimed count separately, publishes null verified count, and does not hide the original failure.

Outputs must be distinct, nonexisting, contained under the explicit run root, disjoint from assets and free of input/ancestor aliases. Final files use exclusive creation and independently created parents. Every resource close is attempted; close failures are recorded without preventing later closes. Main-level duplicate closes were removed. The shell wrapper forwards explicit inputs to the actual pure preflight before invoking the specified launcher, with no early mkdir or fake success artifact.

### Source alpha-filter correction

Independent source comparison showed adapter feedback used the clipped output, whereas Gym retains the unclipped alpha recurrence. The approved small `command_delay.py` change separates raw filter state from bounded executed output. Input clipping remains [-3,3], alpha remains .5, body bounds and no-queue behavior remain unchanged. Smoke/ACSI raw-state mirrors and ordinary PPO's explicit filter state follow the same recurrence; debug output distinguishes filtered versus executed command. Masked public reset still clears both selected state mirrors without changing neighboring rows.

### Gate separation

Gate A retains complete-tree inventory, Python/static Gym checks and real CPU `rsl_rl` tests. It now emits four distinct real dependency/runtime rows: `isaac_gym_dependency`, `isaac_gym_runtime`, `isaaclab_dependency`, `isaaclab_runtime`. Dependency tests isolate only the external import boundary; no successful import counts as simulation. Both real dependency probes report absent here and both runtime rows report not executed.

## Verification commands and exact results

All commands below ran from the worker with the dedicated `../sea-nav-cpu-venv/bin/python`, Python 3.10.12 and Torch 2.6.0+cpu; no system dependency changes.

```bash
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH="$PWD/training/rsl_rl" \
  ../sea-nav-cpu-venv/bin/python -m pytest -q -p no:cacheprovider \
  tests training/rsl_rl/tests \
  sea_nav_current_isaaclab_full_method/tests/gate_a_static_contract.py
```

- Final precommit candidate: **294 passed in 26.88 s**.
- Repeated after commit `844514929726a1cade3303867cc11702b710a04d`: **294 passed in 27.21 s**.
- The source/test tree did not change between these runs. Registration-only dirt is explicitly excluded from source evidence.

```bash
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH="$PWD/training/rsl_rl" \
  ../sea-nav-cpu-venv/bin/python tools/gate_a.py --repo-root "$PWD" \
  --report ../task-6-worker-gate-a-8445149.json
bash -n sea_nav_current_isaaclab_full_method/run_full_method_runtime_smoke.sh
git diff --check
git diff --cached --quiet
```

Gate A output: `{"report":"../task-6-worker-gate-a-8445149.json","status":"passed_with_blockers"}`. Physical report is outside the checkout and not committed. Cases: 83 tracked Python files compiled without writing bytecode; 65 baseline package/entry/controller/license/Go2 files present and nonempty; 20 Gym package files parsed with nine simulator-bound files static-only; seven CPU-safe packages imported; real actor/value/PPO/rollout-storage CPU smoke passed; Gym and Lab dependency/runtime rows each blocked.

Raw report paths (relative to the primary checkout, which is the worker's sibling): `../task-6-worker-gate-a-precommit.json` is historical staged-tree evidence; `../task-6-worker-gate-a-8445149.json` is the post-commit source evidence. Existing independent slice reports are `.codex/delivery/epics/paper-reproduction-80pct/task-6-slice1-review.md` and `task-6-slice1-rereview-1.md`. Pytest stdout is recorded in tool results and the exact summaries above; no additional raw pytest-log file is claimed. The read-only edge subreview returned messages only, not a report file.

Python 3.8 grammar command (no output files):

```bash
PYTHONDONTWRITEBYTECODE=1 ../sea-nav-cpu-venv/bin/python -B -c \
  'import ast, pathlib, subprocess; paths=subprocess.check_output(["git","ls-files","--","*.py"],text=True).splitlines(); [ast.parse(pathlib.Path(p).read_text(),filename=p,feature_version=(3,8)) for p in paths]; print("Python 3.8 grammar passed:",len(paths),"tracked files")'
```

Result: **Python 3.8 grammar passed: 83 tracked files**. Shell syntax and staged/unstaged diff checks passed. Exact staging included source/tests and `task-6-log.md`, not worker registration or external JSON reports; committed tree paths were checked with `git diff --name-status BASE..HEAD` and `git show`.

### Red/green and review history

Detailed command/failure history is in committed `task-6-log.md`; relevant milestones are retained rather than relabeled success:

- Reward/perception RED two missing modules → 17 passed; independent slice review found float32 drift → targeted RED five failed/four passed → repaired full slice 26 passed and parent independent slice rereview PASS.
- Pure environment/path RED two missing modules → ten passed; actual CLI RED eleven missing preflights → eleven and later thirteen passed.
- First in-flight full suite: seven failed/262 passed from inherited old startup/YAML/raw-load/geometry/trace expectations. Migrated only obsolete entry contracts; preserved independent numeric CBF/replay tests and adjacent historical manifest identity assertions.
- Stronger preload-identity + ordinary history/ACSI tests exposed six old purge failures and missing actual ordinary methods; corrected with real bundled-class/consumer tests. One capability test initially expected the wrong exception class; corrected to the explicit blocked RuntimeError.
- Read-only output edge review found alias/overwrite P1 and threshold/close/nested-manifest P2. New RED seven failures → path/receipt/close changes included in 45-pass narrow suite. Follow-up review found existing claimed trace rows caused a second throw on verify failure; RED one failure → six manifest tests passed after null verified-count handling.
- Later full runs: one failed/279 passed (obsolete shell help), one failed/290 passed (eager manifest import introduced while moving shared binding), then 291 passed. The original lazy-import test was preserved and the implementation fixed.
- Filter saturation test had two test-only numeric-literal syntax collection errors before genuine RED one failure on clipped recurrence. Shared/ordinary filter and wrapper fixes → 26 runtime/filter/replay-wiring tests passed; final full 294 passed.

## Task 7 interface / remaining boundaries

- `RuntimePaths.checkpoint_manifest` is canonical, exists when supplied, and is contained under asset_root. `preflight()` parses `--checkpoint-mode {fresh,resume,warm_start,inference}`; any nonfresh mode, explicit manifest, Gym play entry or legacy raw loading flag is currently rejected with a Task 7 blocker before proprietary import. Task 7 should replace this explicit loading blocker with its schema-v2 manifest loader, carry selected mode into the final request/artifact, and retain the existing no-raw-pickle bypass.
- Adapter trainers now create runners without raw load/layer replacement; Gym registry's former load branch is blocked. OnPolicyRunner's save/load and explicit registry migration remain Task 7, not this commit. Legacy play's overrides are unreachable until that task resolves inference and post-override effective configuration.
- `torch._assert_async` here is an eager CPU perception capability check. It does not establish old Gym Torch compatibility, recoverable CUDA exceptions, script/save/load preservation, or scripted policy export.
- CPU tests and AST blocks establish mathematical and call-order contracts, not actual carrier contact mapping, reset side effects, lifecycle, controller interface, timing under CUDA or real robot safety. Source controller startup remains deliberately blocked; no capability bit was promoted to verified.
- Formal 100-trial evaluation, IsaacLab/Gym installation and real runtime, controller provenance/interface acceptance, public redistribution rights, two-Go2 deployment and `paper_v1` acceptance remain blocked/deferred. No paper label/tag, main/stable promotion, remote write or deployment occurred.
- Whole-task independent review and clean frozen project-candidate Rungs 0–2 still belong to the controller. This worker candidate should not be described as final completed recovery until those steps and Task 7 are handled.
