# Task 7 fix1

BASE `92ab65d23590256f3165d8cad6e64698cf11bfe8`; parent-authorized sole writer `/root/review_batch7_callers`.

## Scope

- Checkpoint utility, runner load, runtime model-only load, both adapter training scripts.
- New `tests/test_checkpoint_application.py`, narrowly necessary existing checkpoint test assertions.
- This local ledger; root task_plan/resume_state registration additions remain uncommitted and preserve the old writer's original diff.
- No converter source/absence-contract changes, CBF math, primary integration, remote push or runtime actuation.

## Plan

1. Complete: inspect independent four findings and real consumer fixtures; add focused RED regressions.
2. Complete: shared strict model/geometry validation and transaction rollback; actual-target Adam validation and disposable next-step execution.
3. Complete: payload byte receipt; focused126 passed, final full474 passed54.33s; precommit Gate A five CPU/static passes and four runtime/dependency blockers.
4. Deliver one functional fix commit containing this record, then exact path/identity readback, postcommit checks and parent rereview handoff. Independent acceptance remains pending.

## Acceptance

Model, optimizer, iteration and algorithm/group LR unchanged on every covered load failure in inference/warm_start/resume and model-only consumer.
Wrong keys/shape/dtype/layout/CBF geometry and incompatible Adam reject before live mutation. Valid Adam resume performs next real PPO update.
checkpoint_bytes describes payload, not JSON manifest. Converter remains absent. CPU-only evidence is not simulator or hardware acceptance.
