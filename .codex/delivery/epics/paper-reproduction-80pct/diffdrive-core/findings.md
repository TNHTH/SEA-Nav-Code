# Findings and current truth

- Current artifact: final package candidate delivered by the functional commit containing this record, based on detached BASE `399ce2b08eac40865fd6496d19324f73a3e6cc7e`; original tree was clean before registration. Resolve its fixed OID with Git, not an embedded self-referential hash. No Go2/rsl_rl/Task 7 changes.
- CPU environment: sibling `sea-nav-cpu-venv`, Python 3.10.12, Torch 2.6.0+cpu, pytest 8.4.2.
- Existing SEA actor and CBF require [B,3] body commands and fixed Go2 observation layout; they remain unchanged.
- New package must not use the `rsl_rl` import/distribution name; DashGo has its own RSL-RL implementation.
- New CBF works at lookahead point [l,0], with q=[v,l*omega], metric obstacle points transformed from LiDAR to base, and conservative footprint+l+margin envelope.
- State/controls are instantaneous geometry, not a dynamics or hard-safety certificate. Damping leaves an active residual negative.
- Paper-v1 source resolution verified: shield=.1*(intervention+mean(relu(.1-alpha)^2)); Lreg=range+.05*actor_MSE+.005*critic_MSE, lambda_reg=1. No alpha penalty coefficient of one is imported from upstream.
- Initial implementation validation was package155/combined497/wheel153/sdist155; it is historical and superseded by the P2 closeout below.
- All-invalid rows are rejected; masked invalid range/angle placeholders are sanitized before geometry so pruned-ray outputs and finite masked gradients agree.
- Active constraint and actual nonzero correction are distinct: symmetric zero composite gradient can have eta>0 with identical output and still-negative residual. Both flags are explicit and tested.
- The initial packaging receipts under `/tmp/sea-nav-core-packaging.MeXwyx/` are historical and superseded; temporary artifacts are not committed or durable backups.
- Remaining: independent fixed-commit review/integration by parent; differential-drive consumer integration and all simulator/hardware/performance/evaluation evidence remain unverified. No hard safety or original-paper claim.

## P2 fix-round findings — 2026-09-08

- The original package now carries a scoped MIT `LICENSE`, SPDX source identifiers, PEP 639 `license = "MIT"`/`license-files` metadata, and author attribution. This does not expand any license claim to the containing repository or third-party projects.
- Platform v2 identity contains `wheel_radius_m=.0632`, `track_width_m=.342`, `max_wheel_velocity_rad_s=5.0`, `max_linear_acceleration_mps2=1.0`, `max_angular_acceleration_radps2=.6`, units in field names, and pinned DashGo repository/path provenance including `src/dashgo_rl/control/differential_drive.py`. Every field participates in strict canonical JSON/SHA-256 identity and persistent numeric configuration.
- `RawSafetyObservationSpec` is independent of the normalized 246-dimensional policy ABI. It binds actual ordered ray angles, raw metres/radians/seconds units, shapes, validity, sensor frame, maximum age and semantics. CBF calls require its exact manifest hash and validate shape, angle geometry, range, mask and age. A trusted clear/no-hit return is represented by finite `range_max_m` plus true validity (matching a consumer with `depth_clipping_behavior=max`); valid NaN/Inf/non-positive, stale and all-invalid rows reject.
- CBF configuration identity includes complete platform and raw-safety manifests plus `kappa`, fixed damping and four command stages. Six immutable buffers persist configuration hash, platform hash, safety hash, canonical receipt, numeric configuration and ray angles. Incompatible/corrupt eager state restore is rejected without changing target identity; TorchScript save/load retains exported metadata and expected-identity checks.
- Projection now distinguishes `nominal_body_twist`, `cbf_body_twist`, `platform_projected_command` and separately observed `executed_command`. It applies body-speed clamp and per-axis acceleration limit, then advances from previous feasible wheels toward the acceleration-limited target by one common maximal line-segment scale inside the `[-5,5] rad/s` wheel box, followed by inverse conversion. Convexity preserves body speed, per-axis acceleration and wheel bounds simultaneously; scaling absolute target wheels toward zero does not preserve acceleration for nonzero previous commands. Diagnostics expose both intermediate commands/wheels and the scale; projected and executed residuals use the final inverse-converted twist. Receipt identity names `differential_drive_feasible_segment_v1` and its exact order.
- Stable regression oracle: front single-ray CBF transforms nominal `v=.3` to `v=.1` with residual `-.2`; previous executed `v=.3`, `dt=.05 s`, and `1 m/s^2` deceleration permit only projected `v=.25`, whose recomputed residual is `-.35`. This is diagnostic evidence that stage residuals differ, not a hard-safety proof.
- `paper_v1_shield_loss` alone owns/applies `lambda_shield=.1`; profiles expose inclusion flags and helper-owner identity rather than another `.1` multiplier. Lreg helper similarly owns its fixed internal coefficients. The four profile names/flags remain unchanged.
- Final CPU evidence: package **249 passed in 2.45s**; combined existing SEA plus package **591 passed in 35.95s**. A deterministic 256-command float64 zero-previous probe matched pinned DashGo outputs bit-for-bit; the final segment method intentionally differs under nonzero wheel saturation to preserve acceleration. The supplied counterexample and 696 platform-valid cases selected from 1028 boundary/random wheel candidates verify simultaneous final bounds. Final extracted sdist **249 passed in 2.54s**; isolated installed wheel focused suite **246 passed in 1.57s** plus import/metadata/TorchScript identity smoke.
- Final temporary artifacts: `/tmp/sea-nav-core-feasible-final.SAeHP3/dist/`; wheel SHA-256 `e7705c01f4728f0c26c46497ebf62b41b20d7a6e70364d008ac7380a8846690d`, sdist SHA-256 `9b4f3c4403adcfa73c73f88b2cf3b185bbb80b9b003555fa6bb0c0231aa70498`. Both contain `LICENSE`; wheel metadata reports `sea-nav-core==0.2.0`, `License-Expression: MIT`, `License-File: LICENSE`, noreply author identity and `torch>=2.1`. Installed configuration receipt SHA-256 is `e8526e94a63c6bba3a5d3937bf127cf7e3aba6f891e553dd4852cbc97320fef0` for the smoke-test inputs.
- Ruff was not installed in the task CPU environment, so its optional lint command was unavailable. `git diff --check`, Python imports, exhaustive package tests and the combined CPU suite are the enforced static/behavioral gates.
- Remaining/unverified: independent re-review and primary integration; Isaac Gym/Lab runtime, simulation physics, formal ablation metrics, ROS deployment, real-time budget and hardware. No simulator or robot was started.
