# Task 5 implementation log

Worker: `/root/implement_batch5`. Detached base: `5b99f456fbfb9eb2e6fc8eae5a03a1aa584e2b43`.

Implemented compact `new_replay_episode_v1` snapshots under packaged `rsl_rl/replay`, adapter compatibility boundary, shared ACSI equations/decisions/profile projection, complete reset transaction and masked reconstruction through the Gym base epilogue and adapter lifecycle. No histories are ring payload. Cross-episode curriculum is retained and updates on normal/fallback episode reset after selection, once using saved terminal distance.

RED evidence: initial replay/ACSI/partition collection failed because `rsl_rl.replay` did not exist; lifecycle collection failed for absent transaction/bootstrap APIs; actual runtime AST checks failed for missing terminal capture, shared decisions and reconstruction wiring. Later focused RED regressions exposed a cross-ring reservation acceptance, overwritten slots with reused producer step IDs, cancellation rejected after context-generation change, and a reservation outside requested reset IDs. Each was fixed and its focused test rerun GREEN. Public filter-mirror and bad-mask regression mutations both failed as expected before the fixes were restored.

Initial GREEN milestones: 10 ring/ACSI/partition tests; 9 lifecycle/performance tests; 40 combined focused replay and inherited Gate A cases. Subsequent self-review added whole-bootstrap poison fixtures, xyzw/wxyz rotation, public masked filter recurrence, inclusive random undo endpoints, ring ownership/write-version validation, and rejection of unsupported reset consumers. Final counts/commands and commit identity are in the primary `task-5-report.md`.

Hot path: indexed slot writes only; three per-slot int64 identities plus O(envs) metadata. Additional absolute per-env write counters detect overwritten reservations even if a bad producer repeats a step ID. Fixed active-row work is checked at 4/2,048 environments and 8/4,096 slots. Positive controls catch selected-history clone/index, full clone/roll/cat/stack/where and scalar extraction while accepting views. This is Torch CPU operator-semantic evidence, not CUDA timing.

Narrow inherited-test migrations: Gate A replay records now use explicit geometry and valid physical quaternions. The old arbitrary room/step dictionary and history payload were not a certified snapshot schema. Scalar/batched sample shapes remain compatible, with `_selection` reservation ownership; callers must acknowledge or cancel before sampling the same row again. Only replay-related source tokens in its trainer surface test changed; observation, CBF, manifest and other regressions remain intact.

Runtime limitations: Rung 3G and 3L are blocked. Replay-enabled startup requires a declared `replay_reset_reconstruction_v1` delta and an external runtime contract for terminal capture before carrier auto-reset, stable scene/controller/frame context, masked contact/actuator reset and cache/FK refresh. No fake simulator imports or physical-write assertions were used. The owned trainer's legacy `run_forced_replay_reset_smoke` now explicitly blocks: its prior loop repeatedly captured one unchanged physical boundary, so its arbitrary time labels were not valid replay/runtime evidence. A real multi-env physics/readback smoke remains required.

Task 6 activation bridge: Gym reads `cfg.resolved_run_config`; the adapter constructor accepts `resolved_config=None`. `replay_configs_from_resolved` validates existing projections and required delta, then constructs the replay and ACSI consumers. Task 6 still owns full startup/manifest, regular reward timing, and timestamped perception. Bootstrap is declared noise-free synthetic step 0; it does not fabricate sensor prehistory or rewind curriculum/RNG.

Verification uses `PYTHONDONTWRITEBYTECODE=1`, `PYTHONPATH=$PWD/training/rsl_rl`, and `../sea-nav-cpu-venv/bin/python -m pytest -q -p no:cacheprovider`. Full explicit suite is `tests training/rsl_rl/tests sea_nav_current_isaaclab_full_method/tests/gate_a_static_contract.py`. Gate A output is kept outside the checkout. No compileall, dependency changes, named branches, pushes, worktree removal or unrelated source edits.

Final full explicit suite: **202 passed in 11.62s**. Standalone inherited Gate A: **16 checks passed**, output `/tmp/sea-nav-task5-gate-a.ryXCq4/gate-a.stdout.txt`. Python 3.8 grammar parse: **8 source files passed**. `git diff --check`: clean. Final hot-path counters after write-generation hardening: **58 indexed source scalars, 18 named materializer output scalars, zero scalar extraction**, identical across all three environment/capacity comparisons. Portable `tools/gate_a.py` staged-file evidence is recorded in the primary report.

Portable staged Gate A: `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=$PWD/training/rsl_rl ../sea-nav-cpu-venv/bin/python tools/gate_a.py --repo-root "$PWD" --report /tmp/sea-nav-task5-gate-a.ryXCq4/portable-staged.json` → exit 0, **passed_with_blockers**. Passed: 72 tracked Python files compiled without output, 65 baseline tree assets, 20 Gym files parsed (8 simulator-bound/static-only), 7 CPU-safe packages imported, actor/value/PPO/storage smoke. Isaac Gym Preview 4 runtime remains **blocked** because it is not installed.

## Independent review fix round 1

Addressed the four P2 findings against `53c479d`: active adapter normal/fallback reset now calls preparation before carrier/manual state writes and refresh afterward, before reconstruction; the selected carrier timeout signal excludes replay while play/evaluation mode that ignores carrier signals stays unchanged; collision accumulation remains device-side without hot-path host scalar extraction, with native integer conversion at both result JSON boundaries; cancellation provenance retains one immutable latest event per row (episode, task generation, token and reason) instead of an unbounded lifetime-token dictionary. `cancel_reasons` is a bounded compatibility snapshot. No full event history is claimed.

Targeted RED command: `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=$PWD/training/rsl_rl ../sea-nav-cpu-venv/bin/python -m pytest -q -p no:cacheprovider tests/test_collision_replay_cpu.py tests/test_replay_runtime_wiring.py -k 'cancellation_history or normal_fallback_hooks or selected_carrier_timeout or result_boundaries'` → **7 failed, 1 passed, 13 deselected in 1.50s**. Missing hooks, wrong effective timeout mask, all four zero/nonzero × result-boundary JSON cases and the 1,001-entry retention reproduced the review. After fixes the same command → **8 passed, 13 deselected in 1.51s**.

Covering suite: the six Task 5 test files plus inherited Gate A → **59 passed in 2.16s**, with additional failed normal-prepare/refresh lifecycle cases verifying no ack/cancel/observation after fallback failure. The test uses real extracted pure terminal-selection and reporting expressions; no simulator stack or fake simulator module is loaded. Python 3.8 grammar parse passed for both changed sources; `git diff --check` passed. No full 202-test suite was rerun in this fix round. Portable staged Gate A `/tmp/sea-nav-task5-fix1-gate-a.An0o27/portable-staged.json` → exit 0, **passed_with_blockers**: 72 syntax files, 65 baseline assets, 20 static Gym files, 7 CPU-safe package imports and actor/value/PPO/storage smoke passed; Isaac Gym remains unavailable. Fix commit ID is recorded in the primary report addendum. Real Gym/IsaacLab hooks and physical behavior remain blocked.
