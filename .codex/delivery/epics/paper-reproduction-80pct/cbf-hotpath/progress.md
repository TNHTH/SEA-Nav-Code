# CBF hot-path progress

## 2026-09-08 — activation

- Wrote the separately authorized Task7 fix2 rereview receipt in primary; no primary source/ref changes.
- Git preflight verified primary clean beforehand, exact accepted a453b15c7b4e84f97bc37308f682fdde79e6fbbc, test branch, configured approved TNHTH author, existing remotes, no test upstream (expected `rev-parse @{upstream}` exit 128). Worktree inventory before/after recorded; created only new detached `work/SEA-Nav-Code-cbf-hotpath` from that OID. No named branch, push, cleanup or configuration changes.
- Read git-guru, planning-with-files, root AGENTS and all three CBF review/probe/followup documents; inspected actual core/adapter/exporter/tests and final verification protocol.
- Registered exact six owned commit paths and two uncommitted local registration paths. Existing shared coordination and retained worktrees preserved.
- Next: focused operator-count RED, then minimal implementation and boundary tests. Runtime smoke remains required but unavailable, not replaced by CPU stand-ins.

## Focused RED

Command (CBF worktree; dedicated absolute CPU Python): `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH="$PWD/training/rsl_rl" /home/twyc/Documents/Codex/2026-09-04/https-github-com-tnhth-sea-nav-2/work/sea-nav-cpu-venv/bin/python -m pytest -q -p no:cacheprovider tests/test_cbf_hotpath.py`.

Initial nine budget regressions ran against unchanged production BASE. The six core/adapter/ablation B=2/2048 cases show five scalar extractions, four sums, one norm, and zero/two adapter minima instead of one/three/zero/zero. Real actor/PPO collection shows two calls but ten layer-local extractions, eight sums, two norms and zero/four minima; all nine tests fail for the expected old hot-path budgets. No timing or simulator claim. Next stage: production checked predicate/math split, then add broad compatibility cases.

## First GREEN and expanded dtype probe

- Initial RED: 9 failed in 1.52s, exit 1, all expected budgets.
- After shared math/intermediate split, ordinary adapter override and aggregate checked predicate: same command plus unchanged `training/rsl_rl/tests/test_cbf_lse_layer.py tests/test_cbf_shield.py training/rsl_rl/tests/test_ppo_action_state_identity.py` returned 71 passed in 1.71s, exit 0.
- Added exact legacy short-circuit oracle, native sparse/quantized/complex/meta cases, malformed/dynamic invalids, both script/save/load APIs, seven fixtures' command/gradient equivalence, independent footprint preprocessing and real AST-isolated Gym exporter: focused file returned 50 passed in 2.07s, exit 0.
- A follow-on native Float8 probe (`NaN u`, Float8_e4m3fn rays, positive alpha) found the initial candidate still raised backend `RuntimeError: isfinite not implemented` instead of the original earlier finite-input ValueError. This is a candidate-only gap, not a baseline defect. Add regression and restrict aggregate eligibility to historically supported real numeric dtypes; other dtypes fall back to original validation, never acquire a new acceptance ban.
- Expanded dtype RED command: focused file with `-k 'eager_exact or fallback'` -> 9 failed, 41 deselected in 1.33s, exit 1, expected Float8 precedence/cost gap. Optional Float8_e4m3fn/Float8_e5m2/uint16/uint32/uint64 cases included when supported by installed Torch.
- Repaired aggregate metadata eligibility, preserving all old validation paths; repeated focused plus unchanged CBF/adapter/PPO files -> 112 passed in 2.36s, exit 0. New focused file alone has 50 test cases, with internal invalid/dtype matrices. Real script/save/load and actual AST exporter both pass for core, zero-footprint adapter and named .55 m ablation. Next: exact old-source gradient comparison, fresh full suite and Gate A, staging/commit inventory.

## Complete precommit verification

- Read real core source via `git show a453b15c7b4e84f97bc37308f682fdde79e6fbbc:training/rsl_rl/rsl_rl/modules/cbf_lse_layer.py` into a transient Python namespace. Read the actual old adapter class AST from the same exact commit, binding its base class to that real old core. No persistent source or fake mathematical oracle was created. For core/adapter/.55 m ablation, each of seven committed fixtures was evaluated with command-only, diagnostics-only and joint squared losses: all 63 before/after output/diagnostic/input-gradient comparisons were bit-exact (`atol=rtol=0`), exit 0. This compares actual BASE implementation rather than the test's boundary-only legacy helper; the unchanged golden fixtures remain the independent numerical oracle.
- Complete command: `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH="$PWD/training/rsl_rl" /home/twyc/Documents/Codex/2026-09-04/https-github-com-tnhth-sea-nav-2/work/sea-nav-cpu-venv/bin/python -m pytest -q -p no:cacheprovider tests training/rsl_rl/tests sea_nav_current_isaaclab_full_method/tests/gate_a_static_contract.py` -> **579 passed, 2 skipped in 56.38s**, exit 0, no deselections. Only the two inherited real-CUDA RNG tests skip on this CPU host; protected rejection tests run unchanged.
- After staging the exact six candidate paths, Gate A: same env/interpreter with `tools/gate_a.py --repo-root "$PWD" --report /home/twyc/Documents/Codex/2026-09-04/https-github-com-tnhth-sea-nav-2/work/cbf-hotpath-precommit-gate-a.json` -> `passed_with_blockers`, exit 0; physical JSON inspected. Five passes: 91 tracked Python files compiled without output, 65 baseline paths present/nonempty, 20 Gym files parsed (9 static-only), seven CPU imports, real actor/value/PPO/storage CPU smoke. Four explicit blockers: missing isaacgym and isaaclab dependencies, real Gym and Lab lifecycle smoke not executed. External JSON is local precommit evidence, not committed or published and not a clean frozen-candidate report.
- All 91 tracked Python files pass `ast.parse(..., feature_version=(3,8))` under Python 3.10.12 without bytecode. Accepted BASE and `1c5675b` ancestry pass; `fbce672c` is a retained upstream reference, not an ancestor of this existing recovery history (exit 1, no history mutation). No deleted paths against accepted BASE, `1c5675b`, or upstream `fbce672c`. No untracked/ignored test output. Exact staged six-path inventory confirmed; only the two root registration files remain unstaged.
- Strengthened the existing six operator test cases to assert explicit diagnostic API retains its intended one-check/four-sum/one-norm and adapter two-min budget. No production changes since the complete run; rerun focused/full after this test-only assertion before committing.
- Final precommit repeat of the exact full command after that assertion: **579 passed, 2 skipped in 56.65s**, exit 0, no deselections. Only six candidate paths will be committed; staged check, commit and postcommit full/Gate A results are to be recorded in the two excluded local registration files and controller handoff, rather than mutating the candidate after its identity is known. Repository-scoped approved TNHTH author/committer settings verified against the accepted parent.

## Remaining costs and release boundaries

One aggregate host decision and five device predicate reductions remain on every valid dense real supported call; boolean tensor conjunctions are added. Invalid supported inputs perform aggregation plus the original error checks (NaN-u: 2; zero-rays: 5; zero-alpha: 6 observed scalar extractions). Unsupported metadata follows only the old validator (earlier NaN-u: 1), preserving errors without a new dtype ban. Ordinary command costs three required mathematical sums and no residual/norm/minimum diagnostics. Explicit diagnostics still compute those requested fields. Wrapper raw-logit validation remains separate and alpha is transformed exactly once; wrappers were not edited.

No CUDA timing/throughput, actual CUDA mixed-device execution, legacy PyTorch runtime, proprietary Gym/Lab lifecycle, controller provenance, physical replay, formal paper metrics, rights or hardware acceptance is established. The original P2 disposition belongs to independent fixed-commit review; this writer does not grade its own fix or authorize integration. Branches/push and standalone differential-drive code remain untouched.
