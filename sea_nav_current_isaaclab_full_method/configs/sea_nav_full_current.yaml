schema_version: 1
algorithm_profile: upstream_fbce672c
# Explicit declarations; a missing delta is rejected, never injected.
implementation_delta: [ppo_state_identity_repair, replay_reset_reconstruction_v1]
runtime:
  timeout_seconds: 60.0
  num_envs: 1
  seed: 42
  command_filter_alpha: 0.5
  enable_collision_replay: false
  replay_ring_buffer_steps: 180
  replay_undo_min: 100
  replay_undo_max: 150
  source_max_goal_level: 10.0
